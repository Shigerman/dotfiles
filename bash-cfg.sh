# to make the output of ls command more readable
alias ll='ls -l -h -A -F --color $@'

# to prevent python from creating files with bytecode
export PYTHONDONTWRITEBYTECODE=1

# to edit merge conflicts in vscode, not to open vim
export EDITOR=code
